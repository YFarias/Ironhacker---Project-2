<ul>
  {{#each gameListDB}}
      <li> 
        <a href="/games/{{this._id}}"> {{ this.title }} </a>
        <form action="/games/{{this._id}}/delete" method="POST">
          <button type="submit">Delete</button>
        </form>
      </li>
  {{/each}}
</ul>